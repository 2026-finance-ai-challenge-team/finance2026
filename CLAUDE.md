# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 저장소 상태

현재 두 갈래가 있다. `apps/web`은 React 19 + TypeScript + vinext/Vite 기반 합성 클릭 데모이고, `ocr_test/`는 CLOVA OCR General API에 실제로 연결된 문서 인식 하네스다. 규칙 엔진과 LLM은 아직 없다.

파이썬 작업은 저장소 루트의 `finance` 가상환경을 쓴다. 의존성은 루트 `requirements.txt`.

- 파이썬 환경: `finance/Scripts/python.exe` (설치: `pip install -r requirements.txt`)
- OCR 테스트: `finance/Scripts/python.exe ocr_test/test_parse.py`
- OCR 실행: `finance/Scripts/python.exe ocr_test/run_ocr.py <경로> --mask`

`ocr_test/.env`에 NCP 자격증명이 있다. `.gitignore` 대상이며 값을 코드·문서·커밋에 옮기지 않는다.

- 개발: `cd apps/web && npm run dev`
- 빌드: `cd apps/web && npm run build`
- 린트: `cd apps/web && npm run lint`
- 서버 렌더 검사: `cd apps/web && npm test`

**`AGENTS.md`가 이 저장소의 규범 문서다.** 제품 범위, 판정 규칙, 대회 제출 요건, 개인정보 원칙이 전부 거기 있다.
아래는 그 문서 중 **코드 작성 시 실제로 구속력이 있는 부분**만 추린 것이다. 기획·문구·일정 관련 판단이 필요하면 `AGENTS.md`를 직접 읽는다.

## 아키텍처의 핵심: AI와 규칙 엔진의 역할 분리

이 서비스의 설계 전체가 이 한 줄에서 파생된다.

- **LLM/OCR이 하는 일**: 비정형 PDF·이미지의 문서 분류, 필드 추출(발급기관·명의·발급일·유효기간 등), 쉬운 설명 문장 생성
- **규칙 엔진이 하는 일**: 최종 준비 상태 판정. 기관·목적별 필수/조건부/대체 서류, 유효기간, 교차 확인 필드를 구조화한 **버전형 데이터**로 결정한다

LLM 추론만으로 `준비 완료`를 내면 안 된다. 공식 근거가 없으면 `확인 필요`로 남긴다.

판정 결과는 다섯 상태로만 표현한다: `준비 완료` · `추가 필요` · `기한 만료` · `정보 불일치` · `이번 업무에는 불필요`.

규칙 데이터 한 건에는 반드시 **공식 출처 URL + 기준일 + 마지막 확인일**이 붙는다. 출처 우선순위는 공식 페이지 > 공식 고객센터 확인 > 신뢰 가능한 보조 자료 > 모델 추론이며, 하위가 상위를 덮어쓰지 않는다.

## 처리 파이프라인

`업무·기관 선택 → 파일 일괄 업로드 → 문서 인식 → 규칙 대조 → 다섯 상태+근거 → 부족분 발급 안내 → 제출 묶음 ZIP → 채널·지점·출력 안내 → 사용자 데이터 삭제`

구현 시 지켜야 할 제약:

- **파일명으로 판정하지 않는다.** MIME/magic → 내장 텍스트 추출 → 텍스트가 없는 페이지만 OCR 순서. 전체 페이지 OCR은 하지 않고, 같은 파일 결과는 세션 내 재사용한다.
- **원본 불변.** 공식 증명서를 하나의 PDF로 병합하거나 내용을 수정하지 않는다. ZIP은 원본 + 안전한 파일명의 제출용 복사본 + index/checklist로 구성한다.
- **AI 추출 결과는 사용자가 수정 가능해야 한다.** 신뢰도가 낮으면 짧은 확인을 요청한다.
- 구현하지 않은 것(실시간 대기번호, 자동 제출, 전 은행 지원)을 UI나 문서에 가능한 것처럼 표시하지 않는다.

## 개인정보 (생략 금지)

업로드 문서에 주민번호·계좌·소득·체류자격이 들어온다. 데모라는 이유로 가짜 체크박스만 만들지 않는다.

- 기본값은 비회원·일회성 세션, 세션 종료 시 원본과 파생 데이터 삭제 + 사용자의 즉시 삭제 기능
- 로그에 원문·주민번호·계좌번호·인증정보를 남기지 않는다
- 제3자 AI에는 필요한 최소 필드만 전달하고, 사용자 문서를 학습에 쓰지 않는다
- 테스트·샘플은 **합성 데이터만**. 실제 개인정보가 든 파일을 저장소에 넣지 않는다

## 기술 선택

- 현재 웹 스택은 `apps/web/package.json`을 기준으로 한다. 백엔드·OCR·LLM은 미확정이며 한 업무를 완주하는 데 필요한 공급자만 도입한다.
- Python과 Node 산출물이 함께 있으므로 루트 `.gitignore`를 확인한다. 첫 업무 하나를 완주하는 데 필요한 최소 구조만 만든다.
- OCR, 지도, ZIP, PDF 텍스트 추출은 검증된 라이브러리·공식 API를 쓴다. 직접 구현하지 않는다.

## 검증

가장 위험한 오류는 서류가 빠졌는데 `준비 완료`라고 말하는 것이다. 전체 정확도보다 **거짓 준비 완료율**과 누락 탐지 재현율을 우선한다.

은행·목적 조합마다 합성 문서 묶음과 기대 판정표를 하나씩 남기고, 규칙을 바꿀 때 이 정답표로 회귀 검증한다.

## 대회 제약 (코드에 영향을 주는 것만)

- 예선 마감 **2026-09-07 10:00 KST**, 배포 URL은 **2026-09-07 11:00 ~ 2026-09-11 23:59** 무중단 외부 접근 가능해야 한다. `localhost`·사설망 불가.
- 심사자가 로그인·설명 없이 첫 화면부터 결과 ZIP까지 완주 가능해야 한다(`샘플로 체험하기` 경로 권장).
- 심사 기간에는 호환성을 깨는 배포나 데이터 초기화를 하지 않는다. 무료 호스팅의 수면 모드·사용량 한도를 사전에 확인한다.
- 기능명세서에 "구현 완료"로 적은 기능은 외부 환경에서 실제로 작동해야 한다.

## 문서 일관성

서비스명은 **ProofBridge**로 고정. 핵심 메시지가 바뀌면 `README.md`, `AGENTS.md`, Notion 소개 페이지, 대회 기획서·기능명세서, 랜딩 문구를 함께 확인한다. 단, 상세 조사 내용을 README에 복제하지 않는다.

결과 문구는 `승인 가능`·`100% 통과`가 아니라 `공개 기준 사전 점검 완료`로 쓴다. `세계 최초`·`국내 최초` 류 표현은 쓰지 않는다.
